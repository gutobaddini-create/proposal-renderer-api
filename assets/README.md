# Assets

Coloque o logo real do escritório neste diretório com o nome:

```text
logo.png
```

Se `assets/logo.png` não existir, a API gera automaticamente um monograma temporário em `assets/generated_logo.png`.
